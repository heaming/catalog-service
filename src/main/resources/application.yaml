spring.application.name: catalog-service

spring:
  cassandra:
    keyspace-name: catalog
    port: 9042
    contact-points: 127.0.0.1
    local-datacenter: datacenter1
    schema-action: create_if_not_exists
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/my_db?useSSL=false&useTimezone=true&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true&autoReconnect=true
    username: root
    password: user
  data:
    redis:
      host: localhost
      port: 6379
  output:
    ansi:
      enabled: always
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect